---
title: "Daily Notes Index"
description: "TABLE file.day as \"Date\", join(linear_tickets, \", \") as \"Tickets Worked On\" FROM \"Daily Notes\" WHERE file.day >= date(today) - dur(7 days) SORT file.day DESC"
---

# Daily Notes Index

## 📅 Recent Work

### This Week
```dataview
TABLE 
  file.day as "Date",
  join(linear_tickets, ", ") as "Tickets Worked On"
FROM "Daily Notes"
WHERE file.day >= date(today) - dur(7 days)
SORT file.day DESC
```

### Work by Feature
```dataview
TABLE rows.file.link as "Daily Notes"
FROM "Daily Notes"
WHERE linear_tickets
FLATTEN linear_tickets as ticket
GROUP BY ticket
```

## 📊 Productivity Metrics

### Lines of Code This Month
```dataview
LIST sum(rows.metrics.lines_of_code) as "Total Lines"
FROM "Daily Notes"
WHERE file.day >= date(today) - dur(30 days)
WHERE metrics.lines_of_code
GROUP BY true
```

---
*Auto-generated index*
