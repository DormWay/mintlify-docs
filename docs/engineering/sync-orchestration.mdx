---
title: "Sync Orchestration"
description: "Temporal schedule provisioning, smart sync orchestration, workflow dedupe, and the schedule import/removal contract."
source:
  type: "obsidian"
  path: "Engineering/Architecture/SoT - Sync Orchestration & Schedules.md"
  distilled_at: "2026-04-03"
---

# Sync Orchestration

DormWay uses Temporal schedules plus orchestration endpoints to keep city, campus, calendar, and course data fresh without pushing scheduling complexity into clients.

## Schedule Provisioning

Schedule provisioning only happens when `SYNC_SCHEDULES_ENABLED === 'true'` during engine boot.

Current core schedules:

- `sync-hourly-smart`
- `sync-city-4h`
- `sync-daily-force`
- `sync-term-data-enrichment`
- cleanup schedules
- `plugin-runner-5m`

All of these use skip-style overlap behavior so a backlog does not explode into duplicate runs.

## Orchestration Flow

1. Temporal schedule triggers a workflow bridge
2. The bridge calls the API router orchestration endpoint
3. The orchestration service selects candidates
4. City or campus workflows start with idempotent IDs
5. Results eventually land in canonical stores like `service_data`

## Dedupe Rules

- city workflows: `city-sync-${contextId}`
- campus workflows: `campus-sync-${contextId}`
- calendar-triggered workflows use sharded and bucketed IDs
- account-level calendar sync also uses dedupe windows to suppress burst fan-out

## Schedule Import and Removal

| Workflow | Responsibility |
|----------|----------------|
| `scheduleImport.workflow.ts` | Normalize imported schedule data into `time_blocks` |
| `scheduleRemoval.workflow.ts` | Clean dropped-course data out of `time_blocks` and `student_time_blocks` |

This is important because student-facing schedule cleanup cannot assume `student_time_blocks` is a derived-only view.

## Key Files

- `services/engine/src/schedules/syncOrchestratorSchedules.ts`
- `services/engine/src/schedules/cleanupSchedules.ts`
- `services/engine/src/schedules/pluginRunnerSchedules.ts`
- `services/engine/src/index.ts`
- `services/engine/src/workflows/utils/calendarSyncLoadShedding.ts`
- `services/engine/src/workflows/studentWatcher.simplified.workflow.ts`
- `services/engine/src/activities/student.activities.ts`
- `services/engine/src/workflows/syncOrchestratorSchedule.workflow.ts`
- `services/engine/src/activities/syncScheduler.activities.ts`
- `services/api-router/src/routes/cron-routes.ts`
- `services/api-router/src/routes/admin/sync-orchestrator.ts`
- `services/api-router/src/services/sync-orchestrator-service.ts`
- `services/api-router/src/routes/calendar-routes.ts`
- `services/engine/src/workflows/scheduleImport.workflow.ts`
- `services/engine/src/workflows/scheduleRemoval.workflow.ts`
