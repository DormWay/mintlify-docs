---
title: "TermFactory"
description: "Term Factory - Aggregates terms from context_dependencies"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/entities/term/index.ts"
  generated_at: "2026-04-03"
---

# TermFactory

Term Factory - Aggregates terms from context_dependencies

**Source:** `services/shared/dormway-core/src/entities/term/index.ts`

## Methods

### `invalidateCache`

```typescript
invalidateCache(studentContextId: string): Promise<void>
```

Invalidate cache for a student's terms

### `getForStudent`

```typescript
getForStudent(studentContextId: string, term: string, year: number): Promise<ServiceResult<Term>>
```

Get a specific term for a student

### `getAllForStudent`

```typescript
getAllForStudent(studentContextId: string): Promise<ServiceResult<Term[]>>
```

Get all terms for a student
Handles cases where term_year may be NULL by extracting from term_id
Returns terms in chronological order (oldest to newest) for proper navigation

### `getCurrentForStudent`

```typescript
getCurrentForStudent(studentContextId: string): Promise<ServiceResult<Term>>
```

Get the current term for a student
DORM-873: First checks campus config for canonical term, then falls back to calendar logic
During break periods (late December, late May, late July), prefers upcoming term

### `getByTermId`

```typescript
getByTermId(studentContextId: string, termId: string): Promise<ServiceResult<Term>>
```

Get a term by its termId string for a specific student
DORM-781: Enables parameterized term endpoints for term navigation
