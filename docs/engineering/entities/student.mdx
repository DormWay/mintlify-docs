---
title: "Student"
description: "Student entity with lazy-loaded relationships and service_data"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/entities/student/index.ts"
  generated_at: "2026-03-19"
---

# Student

Student entity with lazy-loaded relationships and service_data

**Source:** `services/shared/dormway-core/src/entities/student/index.ts`

## Properties

| Property | Type | Description |
|----------|------|-------------|
| `id` | `string` | Account ID (primary key) |
| `contextId` | `string` | Student context ID - used by BaseEntity for service_data queries |
| `campusId` | `string` | Campus context ID (parent in hierarchy) |
| `cityId` | `string` | City context ID (campus's parent in hierarchy) Used by iOS for Ably channel subscriptions - DORM-746/753 |
| `email` | `string` | Student email |
| `firstName` | `string` | First name (given name) |
| `lastName` | `string` | Last name (family name) |
| `name` | `string` | Full name |
| `contextName` | `string` | Context display name |
| `clerkId` | `string` | Clerk ID |
| `year` | `string` | Academic year (string like "Freshman", "Sophomore", etc.) Primary source: public_data.year (set during onboarding) |
| `major` | `string` | Major Primary source: public_data.major (set during onboarding) |
| `campusName` | `string` | Campus name Primary source: campus context name (joined in query) Fallback: public_data.campus_name |
| `timezone` | `string` | Timezone |
| `avatarUrl` | `string` | Avatar URL |
| `minor` | `string` | Minor |
| `graduationYear` | `number` | Expected graduation year |
| `housing` | `string` | - |
| `dormId` | `string` | - |
| `dormName` | `string` | - |
| `roomNumber` | `string` | - |
| `onboardingComplete` | `boolean` | Whether onboarding is complete Primary source: public_data.onboarding_completed (set by Temporal workflow) Fallback: context_metadata.onboardingComplete |
| `canvasIntegrated` | `boolean` | Whether Canvas is integrated |
| `isActive` | `boolean` | Whether student is active |
| `metadata` | `StudentMetadata` | Full student metadata |
| `createdAt` | `Date` | When created |
| `updatedAt` | `Date` | When last updated |

## Methods

### `getYearDisplay`

```typescript
getYearDisplay(): string
```

Get year display string
Year is now stored as a string (e.g., "Freshman", "Sophomore")

### `isOnboarded`

```typescript
isOnboarded(): boolean
```

Check if student has completed onboarding

### `getCurrentTermCourses`

```typescript
getCurrentTermCourses(): Promise<ServiceResult<unknown[]>>
```

Get courses for the current academic term
Determines current term based on date and returns enrolled courses

### `enrollInCourse`

```typescript
enrollInCourse(courseContextId: string, options: { termId?: string; termYear?: number; }): Promise<ServiceResult<{ enrolled: boolean; }>>
```

Enroll student in a course
Creates a context_dependency record linking student to course

### `dropCourse`

```typescript
dropCourse(courseContextId: string): Promise<ServiceResult<{ dropped: boolean; }>>
```

Drop a course (remove enrollment)
Removes the context_dependency record

### `archiveCourse`

```typescript
archiveCourse(courseContextId: string): Promise<ServiceResult<{ archived: boolean; }>>
```

Archive a course (mark as completed/historical)
Updates the context_dependency metadata to mark as archived

### `getCampus`

```typescript
getCampus(): Promise<ServiceResult<unknown>>
```

Get the campus this student belongs to
Returns full campus data including academic_calendar from campus_configs

### `getCourses`

```typescript
getCourses(options: { term?: string; year?: number; }): Promise<ServiceResult<unknown[]>>
```

Get enrolled courses for a specific term

### `getTimezone`

```typescript
getTimezone(): Promise<string>
```

Get student's timezone from campus

### `isEnrolledIn`

```typescript
isEnrolledIn(courseContextId: string): Promise<boolean>
```

Check if student is enrolled in a specific course

### `getDayPlan`

```typescript
getDayPlan(): Promise<DayPlan>
```

Get today's day plan

### `getContextUpdate`

```typescript
getContextUpdate(): Promise<ContextUpdate>
```

Get latest context update

### `getContextPrediction`

```typescript
getContextPrediction(): Promise<ContextPrediction & { travel?: TravelContext; }>
```

Get latest context prediction with travel enrichment

Enriches cached context prediction with real-time travel detection.
This follows the architecture principle: ALL business logic lives in dormway-core.

### `getOnboardingData`

```typescript
getOnboardingData(): Promise<OnboardingData>
```

Get onboarding data

### `getIntelligence`

```typescript
getIntelligence(): Promise<DailyIntelligence>
```

Get daily intelligence widget data
Uses method: processed_student_daily_intelligence

### `getHealthData`

```typescript
getHealthData(): Promise<HealthData>
```

Get mobile health data (steps, sleep, activity, etc.)
Uses method: mobile_user_health_data

### `getCoursesWithSyllabus`

```typescript
getCoursesWithSyllabus(): Promise<CourseWithSyllabus[]>
```

Get courses with their syllabus data
Used internally for widget data extraction

### `getTasks`

```typescript
getTasks(options: { status?: string; source?: string; uns…): Promise<ServiceResult<{ items: TaskItem[]; total: number; s…
```

Get manual tasks for this student

### `getTaskById`

```typescript
getTaskById(taskId: string): Promise<ServiceResult<TaskItem>>
```

Get a single task by ID (manual, canvas, or syllabus)

### `completeSyllabusTask`

```typescript
completeSyllabusTask(taskId: string, options: { actualMinutes?: number; difficultyRat…): Promise<ServiceResult<{ completed: boolean; taskId: string;…
```

Complete a syllabus task by marking it in assignment_completion

### `getAttendancePolicies`

```typescript
getAttendancePolicies(): Promise<ServiceResult<{ policies: AttendancePolicyWidget[];…
```

Get attendance policies across all enrolled courses

### `getDeadlines`

```typescript
getDeadlines(options: { startDate?: Date; endDate?: Date; typ…): Promise<ServiceResult<{ items: DeadlineItem[]; total: numbe…
```

Get deadlines across all enrolled courses

### `getPolicyHighlights`

```typescript
getPolicyHighlights(options: { criticality?: "high" | "medium" | "al…): Promise<ServiceResult<{ policies: PolicyHighlightItem[]; to…
```

Get policy highlights across all enrolled courses

### `getStudyMetrics`

```typescript
getStudyMetrics(options: { period?: "day" | "week" | "month" | "…): Promise<ServiceResult<StudyMetricsData>>
```

Get study metrics from time blocks

### `createTask`

```typescript
createTask(params: CreateTaskParams): Promise<ServiceResult<TaskItem>>
```

Create a new task

### `updateTask`

```typescript
updateTask(taskId: string, updates: UpdateTaskParams): Promise<ServiceResult<TaskItem>>
```

Update a task

### `scheduleTask`

```typescript
scheduleTask(taskId: string, startTime: Date, endTime: Date): Promise<ServiceResult<ScheduleTaskResult>>
```

Schedule a task to the timeline by creating a time block

### `unscheduleTask`

```typescript
unscheduleTask(taskId: string): Promise<ServiceResult<UnscheduleTaskResult>>
```

Unschedule a task (return to pending, remove time block)

### `quickCaptureTask`

```typescript
quickCaptureTask(rawInput: string, options: QuickCaptureOptions): Promise<ServiceResult<TaskItem>>
```

Quick capture a task (simplified creation for dashboard widget)

### `deleteTask`

```typescript
deleteTask(taskId: string): Promise<ServiceResult<TaskItem>>
```

Delete a task (soft delete)

### `completeTask`

```typescript
completeTask(taskId: string): Promise<ServiceResult<TaskItem>>
```

Complete a task

### `toJSON`

```typescript
toJSON(): Record<string, unknown>
```

Convert to plain object for API responses
