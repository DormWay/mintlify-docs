---
title: "City"
description: "City entity with lazy-loaded relationships and service_data"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/entities/city/city.entity.ts"
  generated_at: "2026-03-19"
---

# City

City entity with lazy-loaded relationships and service_data

**Source:** `services/shared/dormway-core/src/entities/city/city.entity.ts`

## Properties

| Property | Type | Description |
|----------|------|-------------|
| `id` | `string` | City context ID (primary key) |
| `contextId` | `string` | Alias for id - used by BaseEntity for service_data queries |
| `name` | `string` | City name |
| `state` | `string` | US state abbreviation |
| `country` | `string` | Country (defaults to USA) |
| `timezone` | `string` | City timezone |
| `campusCount` | `number` | Number of campuses in this city |
| `campusIds` | `string[]` | Campus IDs in this city |
| `population` | `number` | City population |
| `processingPriority` | `number` | Processing priority |
| `isActive` | `boolean` | Whether city is active |
| `configurationStatus` | `string` | Configuration status |
| `cascadeEnabled` | `boolean` | Whether cascade syncing is enabled |
| `metadata` | `CityMetadata` | Full city metadata |
| `featureFlags` | `Record<string, boolean>` | Feature flags |
| `createdAt` | `Date` | When created |
| `updatedAt` | `Date` | When last updated |

## Methods

### `hasFeature`

```typescript
hasFeature(flagName: string): boolean
```

Check if a feature flag is enabled

### `getMetroArea`

```typescript
getMetroArea(): string
```

Get the metro area name

### `getPublicTransitOptions`

```typescript
getPublicTransitOptions(): string[]
```

Get public transit options

### `getCampuses`

```typescript
getCampuses(): Promise<ServiceResult<unknown[]>>
```

Get all campuses in this city

### `getTotalStudentCount`

```typescript
getTotalStudentCount(): Promise<ServiceResult<number>>
```

Get the count of active students across all campuses in this city

### `getWeather`

```typescript
getWeather(): Promise<WeatherData>
```

Get current weather data

### `getForecast`

```typescript
getForecast(): Promise<ForecastData>
```

Get weather forecast

### `getNews`

```typescript
getNews(): Promise<CityNews>
```

Get city news

### `getTransit`

```typescript
getTransit(): Promise<TransitData>
```

Get transit/traffic data

### `getProcessedContext`

```typescript
getProcessedContext(): Promise<CityContextWidget>
```

Get processed city context widget

### `toJSON`

```typescript
toJSON(): Record<string, unknown>
```

Convert to plain object for API responses
