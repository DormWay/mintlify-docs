---
title: "AppAttestation"
description: "AppAttestation Entity"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/entities/app-attest/app-attest.entity.ts"
  generated_at: "2026-03-19"
---

# AppAttestation

AppAttestation Entity

Represents a registered device attestation for Apple App Attest

**Source:** `services/shared/dormway-core/src/entities/app-attest/app-attest.entity.ts`

## Properties

| Property | Type | Description |
|----------|------|-------------|
| `id` | `string` | - |
| `keyId` | `string` | - |
| `publicKey` | `Buffer` | - |
| `platform` | `"ios" \| "android"` | - |
| `appVersion` | `string` | - |
| `teamId` | `string` | - |
| `appId` | `string` | - |
| `counter` | `bigint` | - |
| `userId` | `string` | - |
| `deviceInfo` | `Record<string, unknown>` | - |
| `createdAt` | `Date` | - |
| `lastUsedAt` | `Date` | - |
| `isRevoked` | `boolean` | - |
| `revokedAt` | `Date` | - |
| `revocationReason` | `string` | - |

## Methods

### `toJSON`

```typescript
toJSON(): Record<string, unknown>
```

Convert entity to JSON representation

### `isValidForUse`

```typescript
isValidForUse(): boolean
```

Check if attestation is valid for use

### `incrementCounter`

```typescript
incrementCounter(): void
```

Increment counter (for replay attack prevention)

### `revoke`

```typescript
revoke(reason: string): void
```

Mark attestation as revoked
