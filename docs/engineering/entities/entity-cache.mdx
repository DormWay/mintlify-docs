---
title: "EntityCache"
description: "Simple in-memory cache with TTL support"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/entities/base/entity-cache.ts"
  generated_at: "2026-03-19"
---

# EntityCache

Simple in-memory cache with TTL support
Used for entity-level lazy loading cache

**Source:** `services/shared/dormway-core/src/entities/base/entity-cache.ts`

## Properties

| Property | Type | Description |
|----------|------|-------------|
| `size` | `number` | Get cache size |

## Methods

### `get`

```typescript
get(key: string): T
```

Get a cached value

### `set`

```typescript
set(key: string, value: T, ttlMs: number): void
```

Set a cached value with optional TTL override

### `has`

```typescript
has(key: string): boolean
```

Check if a key exists and is not expired

### `delete`

```typescript
delete(key: string): boolean
```

Delete a cached value

### `clear`

```typescript
clear(): void
```

Clear all cached values

### `prune`

```typescript
prune(): number
```

Clear expired entries

### `getOrSet`

```typescript
getOrSet(key: string, factory: () => Promise<T>, ttlMs: number): Promise<T>
```

Get or set a cached value using a factory function
