---
title: "CityFactory"
description: "City Factory - Singleton pattern for entity creation and queries"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/entities/city/city.factory.ts"
  generated_at: "2026-03-19"
---

# CityFactory

City Factory - Singleton pattern for entity creation and queries

**Source:** `services/shared/dormway-core/src/entities/city/city.factory.ts`

## Methods

### `invalidateCache`

```typescript
invalidateCache(cityId: string): Promise<void>
```

Invalidate cache for a city

### `getById`

```typescript
getById(id: string): Promise<ServiceResult<City>>
```

Get a city by ID

### `getByNameAndState`

```typescript
getByNameAndState(name: string, state: string): Promise<ServiceResult<City>>
```

Get a city by name and state

### `getByCampusId`

```typescript
getByCampusId(campusId: string): Promise<ServiceResult<City>>
```

Get a city by campus ID

### `list`

```typescript
list(options: CityListOptions): Promise<ServiceResult<PaginatedResult<City>>>
```

List cities with pagination and filtering

### `getByIds`

```typescript
getByIds(ids: string[]): Promise<ServiceResult<City[]>>
```

Get multiple cities by IDs (batch operation)

### `search`

```typescript
search(query: string, limit: number): Promise<ServiceResult<City[]>>
```

Search cities by name

### `findNearestCity`

```typescript
findNearestCity(latitude: number, longitude: number, maxDistanceKm: number): Promise<ServiceResult<{ city: City; distanceKm: number; }>>
```

Find nearest city to given coordinates using PostGIS
Returns the closest city we have data for, with distance in meters

### `getQAStatus`

```typescript
getQAStatus(cityId: string): Promise<ServiceResult<CityQAStatus>>
```

Get QA status for a city - comprehensive view of data quality

### `listForQA`

```typescript
listForQA(options: CityQAFilterOptions): Promise<ServiceResult<PaginatedResult<CityQAStatus>>>
```

List cities that need QA attention
OPTIMIZED: Uses 2 queries instead of N+1 queries
