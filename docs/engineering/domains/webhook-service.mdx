---
title: "WebhookService"
description: "Service for managing webhook subscriptions and dispatching events to external URLs."
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/domains/webhooks/index.ts"
  generated_at: "2026-04-03"
---

# WebhookService

Service for managing webhook subscriptions and dispatching events to external URLs.

**Source:** `services/shared/dormway-core/src/domains/webhooks/index.ts`

## Methods

### `dispatchEvent`

```typescript
dispatchEvent(event: WebhookEvent): Promise<void>
```

Dispatch an event to all active webhook subscriptions that match the event type.
Best-effort: never throws, logs failures per subscription.

### `list`

```typescript
list(): Promise<ServiceResult<WebhookSubscription[]>>
```

List all webhook subscriptions ordered by creation date (newest first).

### `getById`

```typescript
getById(id: string): Promise<ServiceResult<WebhookSubscription>>
```

Get a single webhook subscription by ID.

### `create`

```typescript
create(input: CreateWebhookInput, userId: string): Promise<ServiceResult<WebhookSubscription>>
```

Create a new webhook subscription.

### `update`

```typescript
update(id: string, input: UpdateWebhookInput): Promise<ServiceResult<WebhookSubscription>>
```

Update an existing webhook subscription.
Only provided fields are updated (partial update).

### `delete`

```typescript
delete(id: string): Promise<ServiceResult<boolean>>
```

Delete a webhook subscription by ID.

### `test`

```typescript
test(id: string): Promise<ServiceResult<{ statusCode: number; responseTime: n…
```

Send a test payload to a specific webhook subscription's URL.
Returns the HTTP status code and response time in milliseconds.

### `healthCheck`

```typescript
healthCheck(): Promise<boolean>
```

Health check
