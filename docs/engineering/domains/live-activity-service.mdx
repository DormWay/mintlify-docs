---
title: "LiveActivityService"
description: "Reference for the Live Activity Service implementation."
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/domains/live-activity/index.ts"
  generated_at: "2026-04-03"
---

# LiveActivityService

Live Activity Service Implementation

**Source:** `services/shared/dormway-core/src/domains/live-activity/index.ts`

## Methods

### `register`

```typescript
register(options: RegisterLiveActivityOptions): Promise<ServiceResult<{ id: string; }>>
```

Register a new Live Activity token

### `getActiveForUser`

```typescript
getActiveForUser(userId: string): Promise<ServiceResult<LiveActivityRecord[]>>
```

Get active Live Activities for a user

### `listForUser`

```typescript
listForUser(options: ListLiveActivitiesOptions): Promise<ServiceResult<LiveActivityRecord[]>>
```

List all Live Activities for a user

### `getByActivityId`

```typescript
getByActivityId(userId: string, activityId: string): Promise<ServiceResult<LiveActivityRecord>>
```

Get a specific Live Activity by ID

### `sendUpdate`

```typescript
sendUpdate(options: UpdateLiveActivityOptions): Promise<ServiceResult<void>>
```

Send update to a Live Activity via APNs (through api-router)

### `end`

```typescript
end(options: EndLiveActivityOptions): Promise<ServiceResult<void>>
```

End a Live Activity

### `markStale`

```typescript
markStale(userId: string, activityId: string): Promise<ServiceResult<void>>
```

Mark a Live Activity as stale

### `updateClassCountdowns`

```typescript
updateClassCountdowns(userId: string, nextClassInfo: { classId: string; classTitle: string; …): Promise<ServiceResult<UpdateStats>>
```

Update all class countdown activities for a user
Uses stored metadata if nextClassInfo not provided

### `prune`

```typescript
prune(options: PruneLiveActivitiesOptions): Promise<ServiceResult<PruneResult>>
```

Prune old/stale Live Activities

### `healthCheck`

```typescript
healthCheck(): Promise<boolean>
```

Health check
