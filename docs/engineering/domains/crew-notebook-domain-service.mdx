---
title: "CrewNotebookDomainService"
description: "Reference for the Crew Notebook Domain Service implementation."
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/domains/crew/crew-notebook.domain.ts"
  generated_at: "2026-04-03"
---

# CrewNotebookDomainService

Reference for the Crew Notebook Domain Service implementation.

**Source:** `services/shared/dormway-core/src/domains/crew/crew-notebook.domain.ts`

## Methods

### `listSections`

```typescript
listSections(crewId: string): Promise<CrewNotebookSection[]>
```

### `getSection`

```typescript
getSection(crewId: string, date: string): Promise<CrewNotebookSection>
```

### `upsertSection`

```typescript
upsertSection(crewId: string, date: string, content: string, updatedBy: string): Promise<CrewNotebookSection>
```

### `getHistory`

```typescript
getHistory(crewId: string, date: string): Promise<CrewNotebookHistoryEntry[]>
```

### `search`

```typescript
search(crewId: string, query: string): Promise<(CrewNotebookSection & { rank: number; })[]>
```

### `getNotesAcrossCrews`

```typescript
getNotesAcrossCrews(userId: string, opts: { limit?: number; }): Promise<CrossCrewNote[]>
```

Return recent notebook sections across ALL crews the user belongs to.
Used by the Library surface to show shared crew notes.
