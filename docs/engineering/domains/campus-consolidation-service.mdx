---
title: "CampusConsolidationService"
description: "Campus Consolidation Service"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/domains/campus/campus-consolidation.service.ts"
  generated_at: "2026-03-19"
---

# CampusConsolidationService

Campus Consolidation Service

**Source:** `services/shared/dormway-core/src/domains/campus/campus-consolidation.service.ts`

## Methods

### `findDuplicateCampuses`

```typescript
findDuplicateCampuses(): Promise<ServiceResult<DuplicateGroup[]>>
```

Find all duplicate campuses (exact name match WITHIN same city)
Excludes franchises which legitimately have same name in different cities

### `findDuplicateBuildings`

```typescript
findDuplicateBuildings(campusId: string): Promise<ServiceResult<BuildingDuplicateGroup[]>>
```

Find duplicate buildings within a campus using fuzzy matching

### `findCampusesWithBuildingDuplicates`

```typescript
findCampusesWithBuildingDuplicates(): Promise<ServiceResult<{ campusId: string; campusName: strin…
```

Find all campuses with potential building duplicates
Uses a single optimized query instead of N+1 queries

### `consolidateCampuses`

```typescript
consolidateCampuses(canonicalId: string, duplicateIds: string[], dryRun: boolean): Promise<ServiceResult<ConsolidationResult>>
```

Consolidate duplicate campuses - merge into canonical

### `consolidateBuildings`

```typescript
consolidateBuildings(canonicalId: string, duplicateIds: string[], dryRun: boolean): Promise<ServiceResult<ConsolidationResult>>
```

Consolidate duplicate buildings - keep canonical, delete others

### `getStats`

```typescript
getStats(): Promise<ServiceResult<{ duplicateCampusNames: number; extra…
```

Get consolidation statistics (fast version)
