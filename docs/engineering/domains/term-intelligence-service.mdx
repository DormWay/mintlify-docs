---
title: "TermIntelligenceService"
description: "Reference for the Term Intelligence Service implementation."
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/domains/term-intelligence/index.ts"
  generated_at: "2026-04-03"
---

# TermIntelligenceService

Reference for the Term Intelligence Service implementation.

**Source:** `services/shared/dormway-core/src/domains/term-intelligence/index.ts`

## Methods

### `getEmailIntelligenceBundle`

```typescript
getEmailIntelligenceBundle(studentContextId: string, type: EmailBundleType, date: string): Promise<EmailIntelligenceBundle>
```

Main entry point for email builders.
Fetches normalized intelligence once and applies temporal filtering
to produce a ready-to-render bundle.

### `getWeekWorkloadIntensity`

```typescript
getWeekWorkloadIntensity(studentContextId: string, weekNumber: number): Promise<WorkloadIntensity>
```

Get workload intensity for a specific week (or current week).

### `getFilteredWarnings`

```typescript
getFilteredWarnings(studentContextId: string, startDate: string, endDate: string): Promise<NormalizedEarlyWarning[]>
```

Get warnings filtered to a date range.

### `getFilteredConflicts`

```typescript
getFilteredConflicts(studentContextId: string, startDate: string, endDate: string): Promise<NormalizedConflictItem[]>
```

Get conflicts filtered to a date range.

### `getCrossCourseInsights`

```typescript
getCrossCourseInsights(studentContextId: string): Promise<NormalizedCrossCourseInsights>
```

Get cross-course insights (semester-level, not time-filtered).

### `getThisWeekRecommendations`

```typescript
getThisWeekRecommendations(studentContextId: string, weekNumber: number): Promise<Recommendation[]>
```

Get recommendations for the current week.

### `getTodayRecommendations`

```typescript
getTodayRecommendations(studentContextId: string, _date: string): Promise<Recommendation[]>
```

Get recommendations for a specific day.
