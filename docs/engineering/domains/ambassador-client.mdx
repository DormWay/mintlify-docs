---
title: "AmbassadorClient"
description: "AmbassadorClient reference"
source:
  type: "codegen"
  paths:
    - "services/shared/dormway-core/src/domains/ambassador/ambassador.client.ts"
  generated_at: "2026-03-19"
---

# AmbassadorClient

**Source:** `services/shared/dormway-core/src/domains/ambassador/ambassador.client.ts`

## Methods

### `getAmbassador`

```typescript
getAmbassador(uid: string): Promise<AmbassadorProfile>
```

Get ambassador by UID (platform_id)

### `getAmbassadorByEmail`

```typescript
getAmbassadorByEmail(email: string, options: GetAmbassadorByEmailOptions): Promise<AmbassadorProfile>
```

Get ambassador by email, with optional auto-creation

### `validateReferralCode`

```typescript
validateReferralCode(code: string): Promise<ValidateReferralCodeResult>
```

Validate a referral code

### `recordEvent`

```typescript
recordEvent(params: RecordEventParams): Promise<string>
```

Record a referral event

### `recordAttribution`

```typescript
recordAttribution(params: RecordAttributionParams): Promise<AttributionResult>
```

Record referral attribution
Uses POST /referral_attribution/record/ with QUERY PARAMETERS

### `recordOrder`

```typescript
recordOrder(params: RecordOrderParams): Promise<OrderResult>
```

Record an order (v3 API)
Use this for v3 campaigns to create orders and trigger commissions
POST /order/add/

### `getStats`

```typescript
getStats(ambassadorUid: string, email: string): Promise<AmbassadorStats>
```

Get ambassador statistics

### `generateReferralLink`

```typescript
generateReferralLink(ambassadorUid: string, channel: string): string
```

Generate a tracked referral link

### `updateAmbassador`

```typescript
updateAmbassador(params: UpdateAmbassadorParams): Promise<boolean>
```

Update an existing ambassador (e.g., to set groups)
Uses POST /ambassador/update/ with form-encoded fields.

### `listAmbassadors`

```typescript
listAmbassadors(options: ListAmbassadorsOptions): Promise<ListAmbassadorsResult>
```

List all ambassadors for the campaign (admin)
