---
title: "iOS Telemetry Events Inventory"
description: "This document tracks all telemetry events currently implemented and firing from the DormWayFresh iOS app, plus their status for CustomerIO CRM journey integr..."
---

# iOS Telemetry Events Inventory
*Created: 2025-07-13*
*Last Updated: 2025-07-13*

## Overview
This document tracks all telemetry events currently implemented and firing from the DormWayFresh iOS app, plus their status for CustomerIO CRM journey integration.

## Event Categories

### 🔐 Authentication & Onboarding Events

| Event Name | Status | CustomerIO Integration | CRM Journey | Properties |
|------------|--------|------------------------|-------------|------------|
| `user_signed_up` | ✅ **Firing** | ✅ **Connected** | Onboarding sequence | `provider`, `campus`, `major` |
| `user_logged_in` | ✅ **Firing** | ✅ **Connected** | Daily engagement, welcome back | `provider`, `timestamp` |
| `user_logged_out` | ✅ **Firing** | ✅ **Connected** | Re-engagement campaigns | `timestamp` |

### 📱 App Lifecycle Events

| Event Name | Status | CustomerIO Integration | CRM Journey | Properties |
|------------|--------|------------------------|-------------|------------|
| `app.launch` | ✅ **Firing** | ❌ **Not Connected** | Daily engagement tracking | `state`, `timestamp`, `app_version`, `build_number`, `device_model`, `os_version` |
| `app.background` | ✅ **Firing** | ❌ **Not Connected** | Usage pattern analysis | `state`, `timestamp` |
| `app.foreground` | ✅ **Firing** | ❌ **Not Connected** | Return user engagement | `state`, `timestamp`, `previous_phase`, `sync_performed` |

### 📅 Academic Journey Events

| Event Name | Status | CustomerIO Integration | CRM Journey | Properties |
|------------|--------|------------------------|-------------|------------|
| `schedule_imported` | ✅ **Firing** | ❌ **Not Connected** | Academic setup completion | `import_method`, `course_count`, `source` |
| `async_schedule_imported` | ✅ **Firing** | ❌ **Not Connected** | Advanced feature adoption | `course_count`, `processing_time` |
| `schedule_import_failed` | ✅ **Firing** | ❌ **Not Connected** | Support/help needed | `error_type`, `source` |
| `calendar.sync` | ✅ **Firing** | ❌ **Not Connected** | Calendar integration success | `source`, `event_count`, `calendar_types` |
| `syllabus_uploaded` | ❌ **Not Implemented** | ❌ **Not Connected** | Content upload milestone | TBD |
| `syllabus_processed` | ❌ **Not Implemented** | ❌ **Not Connected** | AI processing success | TBD |

### 📍 Location & Context Events

| Event Name | Status | CustomerIO Integration | CRM Journey | Properties |
|------------|--------|------------------------|-------------|------------|
| `location.campus_entry` | ✅ **Firing** | ❌ **Not Connected** | Campus-based messaging | `region`, `campus_name` |
| `location.campus_exit` | ✅ **Firing** | ❌ **Not Connected** | Campus departure tips | `region`, `campus_name` |
| `location.building_entry` | ✅ **Firing** | ❌ **Not Connected** | Building-specific services | `region`, `building_name` |
| `location.building_exit` | ✅ **Firing** | ❌ **Not Connected** | Location transition | `region`, `building_name` |
| `context_changed` | ✅ **Firing** | ✅ **Connected** | Location-based services | `context_type`, `context_name`, `place_name` |

### 🏥 Health & Wellness Events

| Event Name | Status | CustomerIO Integration | CRM Journey | Properties |
|------------|--------|------------------------|-------------|------------|
| `health.update` | ✅ **Firing** | ❌ **Not Connected** | Wellness milestones | `steps`, `distance`, `active_calories`, `heart_rate`, `sleep_duration` |
| `mobile_mood_update` | ✅ **Firing** | ❌ **Not Connected** | Mental health support | `mood`, `energy_level`, `stress_level`, `note` |
| `screentime.update` | ✅ **Firing** | ❌ **Not Connected** | Digital wellness | `total_time`, `pickups`, `notifications` |

### 🎯 Feature Adoption Events

| Event Name | Status | CustomerIO Integration | CRM Journey | Properties |
|------------|--------|------------------------|-------------|------------|
| `widget_added` | ✅ **Firing** | ✅ **Connected** | Feature discovery | `widget_type`, `context` |
| `first_braingains_query` | ❌ **Not Implemented** | ❌ **Not Connected** | AI assistant adoption | TBD |
| `data_collection_enabled` | ❌ **Not Implemented** | ❌ **Not Connected** | Privacy consent milestones | TBD |

### 🔧 Debug & Test Events

| Event Name | Status | CustomerIO Integration | CRM Journey | Properties |
|------------|--------|------------------------|-------------|------------|
| `debug_test` | ✅ **Firing** | ❌ **Not Connected** | Development only | Various debug properties |
| `debug_force_ably` | ✅ **Firing** | ❌ **Not Connected** | Development only | Connection test data |

## Custom Signal Events

These are comprehensive data packages sent for server-side processing:

| Signal Name | Status | CustomerIO Integration | Purpose | Properties |
|-------------|--------|------------------------|---------|------------|
| `mobile_context_update` | ✅ **Firing** | ❌ **Not Connected** | LLM context processing | Full context aggregation |
| `mobile_health_data` | ✅ **Firing** | ❌ **Not Connected** | Health data processing | Formatted health metrics |
| `mobile_calendar_data` | ✅ **Firing** | ❌ **Not Connected** | Calendar data processing | Calendar events and metadata |

## Implementation Status

### ✅ Currently Working
- **iOS Telemetry**: All events firing correctly via TelemetryService
- **Ably Transport**: Real-time event delivery to server
- **Basic CustomerIO**: Authentication events connected

### ❌ Missing Integrations
- **Temporal → CustomerIO**: Server-side event routing not implemented
- **Academic Events**: Schedule/syllabus events not triggering CRM journeys
- **Wellness Events**: Health/mood events not connected to CustomerIO
- **Feature Adoption**: Missing key feature usage events

### 🔄 Next Steps
1. **Implement server-side CustomerIO routing** in Temporal workflows
2. **Add missing academic events** (syllabus upload, BrainGains usage)
3. **Connect wellness events** to CustomerIO for health journey triggers
4. **Map existing events** to specific CRM journey campaigns
5. **Add feature adoption tracking** for key app features

## Event Transport Architecture

```
iOS App → TelemetryService → Ably Real-time → Engine/Temporal → CustomerIO
                           → HTTP API (backup) → API Router → Engine/Temporal → CustomerIO
```

### Current Implementation
- **iOS → Ably**: ✅ Working
- **iOS → HTTP**: ✅ Working (backup)
- **Temporal Processing**: ✅ Working
- **Temporal → CustomerIO**: ❌ **Missing**

## CRM Journey Mapping

### High Priority for CustomerIO Integration
1. **`schedule_imported`** → "Academic Setup Complete" journey
2. **`mobile_mood_update`** → "Wellness Check-in" journey  
3. **`location.campus_entry`** → "Campus Arrival" journey
4. **`health.update`** → "Activity Milestones" journey
5. **`calendar.sync`** → "Calendar Integration Success" journey

### Medium Priority
1. **`app.launch`** → "Daily Engagement" tracking
2. **`context_changed`** → "Location-based Services" journey
3. **`schedule_import_failed`** → "Support Needed" journey

### Future Implementation
1. **`syllabus_uploaded`** → "Content Upload Milestone" journey
2. **`first_braingains_query`** → "AI Assistant Adoption" journey
3. **`heavy_workload_detected`** → "Academic Stress Support" journey

---

## Change Log

**2025-07-13**: Initial inventory created based on comprehensive iOS app analysis
- Documented 25+ telemetry events currently firing
- Identified CustomerIO integration gaps
- Mapped priority events for CRM journey implementation
