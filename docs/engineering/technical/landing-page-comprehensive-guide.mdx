---
title: "Landing Page Comprehensive Guide"
description: "This document provides a complete overview of the DormWay landing page, including all components, sections, colors, design tokens, and architecture."
---

# Landing Page Comprehensive Guide

**Generated:** $(date)

This document provides a complete overview of the DormWay landing page, including all components, sections, colors, design tokens, and architecture.

## Table of Contents

1. [Page Structure](#page-structure)
2. [Components Overview](#components-overview)
3. [Design System & Colors](#design-system--colors)
4. [Theme System](#theme-system)
5. [Key Features](#key-features)
6. [File Organization](#file-organization)

---

## Page Structure

The landing page (`src/app/page.tsx`) is organized into the following sections:

### 1. **Loading State**
- Shows while checking authentication
- Features Aurora background animation
- Displays loading spinner

### 2. **Aurora Background**
- WebGL-based animated gradient background
- Color stops: `#6b21ff` (purple), `#ff5fa1` (pink), `#ff7b3f` (orange)
- Desktop: WebGL Aurora component
- Mobile: CSS fallback with radial gradients
- Theme-aware (dark/light/glass modes)

### 3. **Hero Section** (`HeroSection.tsx`)
- Main headline and value proposition
- Primary CTA button
- Feature badges (Free forever, Works at 6,134 schools, Built by a student)
- Testimonial quote
- School detection and personalization

### 4. **Sticky Mini Navigation** (`StickyMiniNav.tsx`)
- Sticky navigation bar with section links
- Smooth scroll behavior
- Active section highlighting

### 5. **Get Started Section** (`GetStartedSection.tsx`)
- Three-step setup process
- Visual step indicators
- Call-to-action buttons

### 6. **Problem Section** (`ProblemSection.tsx`)
- Highlights the problem students face
- "You're juggling too many systems"
- Systems cloud visualization

### 7. **Comparison Section** (`ComparisonSection.tsx`)
- Before/after comparison
- Competitor comparison
- Value proposition differentiation

### 8. **Ace Section** (`AceSection.tsx`)
- AI assistant features
- Smart help capabilities
- Context-aware assistance

### 9. **How It Works + Upload** (`HowItWorksWithUpload.tsx`)
- Three-step setup process
- Inline file upload functionality
- Email input and drag-and-drop
- Uses `useInlineFileUpload` hook

### 10. **Social Proof Section** (`SocialProofSection.tsx`)
- Testimonials
- User statistics
- Trust indicators

### 11. **Trusted Campuses**
- Rotating list of school names
- "Available at 6,134 schools" badge
- Auto-rotating display

### 12. **Who Made This**
- Team member profiles
- Riley Crimmins (CEO)
- Ethan Kaplan (CTO)
- Social links and bios

### 13. **iOS Coming Soon**
- Mobile app preview
- Waitlist signup
- Feature highlights

### 14. **FAQ Section**
- Common questions
- Expandable details
- Cost, data safety, account requirements

### 15. **Blog Section** (`HomepageBlogSections.tsx`)
- Recent blog posts
- SEO content

### 16. **Bottom CTA**
- Final call-to-action
- Primary CTA button

### 17. **Footer** (`Footer.tsx`)
- Links and navigation
- Legal information
- Social links

---

## Components Overview

### Landing Page Components (`src/components/landing/`)

1. **HeroSection.tsx** - Main hero section with headline and CTA
2. **PrimaryCTA.tsx** - Reusable primary CTA button component
3. **GetStartedSection.tsx** - Three-step setup section
4. **ProblemSection.tsx** - Problem statement section
5. **ComparisonSection.tsx** - Before/after and competitor comparison
6. **AceSection.tsx** - AI assistant features section
7. **HowItWorksSection.tsx** - How it works explanation
8. **HowItWorksWithUpload.tsx** - How it works with inline upload
9. **SocialProofSection.tsx** - Testimonials and social proof
10. **SystemsCloud.tsx** - Systems cloud visualization component
11. **SectionPattern.tsx** - Reusable section pattern wrapper

### Supporting Components

1. **Aurora.tsx** - WebGL background animation
2. **NavBar.tsx** - Top navigation bar
3. **StickyMiniNav.tsx** - Sticky section navigation
4. **Footer.tsx** - Page footer
5. **WaitlistModal.tsx** - Mobile waitlist modal
6. **CampusAmbassadorForm.tsx** - Campus ambassador signup
7. **HomepageBlogSections.tsx** - Blog content section
8. **RotatingText.tsx** - Rotating text animation
9. **GlassIcons.tsx** - Glassmorphic icon components

### System Components (`src/components/system/`)

1. **AnimatedIcon.tsx** - Animated icon wrapper (Lottie support)

### Hooks (`src/hooks/`)

1. **useInlineFileUpload.ts** - File upload hook for landing page
   - Email validation
   - File validation (type, size)
   - Drag-and-drop support
   - API integration

---

## Design System & Colors

### Aurora Colors

The Aurora background uses these color stops:
- **Purple:** `#6b21ff`
- **Pink:** `#ff5fa1`
- **Orange:** `#ff7b3f`

### Primary Brand Colors

- `--ds-primary-brand`: `#6366f1` (indigo)
- `--ds-primary-soft`: `#818cf8` (lighter indigo)
- `--ds-primary-deep`: `#4f46e5` (darker indigo)

### Semantic Colors

- `--ds-success`: `#10b981` (green)
- `--ds-error`: `#ef4444` (red)
- `--ds-warning`: `#f59e0b` (amber)
- `--ds-info`: `#3b82f6` (blue)

### Typography

- **Font Family (Sans):** `-apple-system, BlinkMacSystemFont, "Inter", "SF Pro Display", system-ui, sans-serif`
- **Font Family (Mono):** `"SF Mono", "JetBrains Mono", "Fira Code", "Consolas", monospace`
- **Font Family (Display):** `-apple-system, BlinkMacSystemFont, "SF Pro Display", system-ui, sans-serif`

### Spacing Scale

- `--ds-spacing-xs`: `0.25rem` (4px)
- `--ds-spacing-sm`: `0.5rem` (8px)
- `--ds-spacing-md`: `1rem` (16px)
- `--ds-spacing-lg`: `1.5rem` (24px)
- `--ds-spacing-xl`: `2rem` (32px)
- `--ds-spacing-2xl`: `3rem` (48px)
- `--ds-spacing-3xl`: `4rem` (64px)

### Border Radius

- `--ds-radius-sm`: `0.5rem` (8px)
- `--ds-radius-md`: `0.75rem` (12px)
- `--ds-radius-lg`: `1rem` (16px)
- `--ds-radius-xl`: `1.5rem` (24px)
- `--ds-radius-2xl`: `2rem` (32px)
- `--ds-card-radius`: `1.5rem`
- `--ds-chip-radius`: `9999px`

### Transitions

- `--ds-transition-fast`: `0.12s cubic-bezier(0.4, 0, 0.2, 1)`
- `--ds-transition-base`: `0.2s cubic-bezier(0.4, 0, 0.2, 1)`
- `--ds-transition-slow`: `0.3s cubic-bezier(0.4, 0, 0.2, 1)`
- `--ds-transition-spring`: `0.4s cubic-bezier(0.34, 1.56, 0.64, 1)`

---

## Theme System

The landing page supports multiple themes:

### Available Themes

1. **System** - Respects user's OS preference
2. **Light** - Light mode
3. **Dark** - Dark mode (default for unauthenticated users)
4. **Glass** - Glassmorphic dark mode

### Theme Configuration

- **File:** `src/lib/theme-config.ts`
- **Context:** `src/contexts/ThemeContext.tsx`
- **Utilities:** `src/lib/theme-utils.ts`
- **DOM Manipulation:** `src/lib/theme-dom.ts`

### Theme Bootstrap

The theme system is bootstrapped in `src/app/layout.tsx` with:
- Theme configuration data
- Design tokens
- Accent presets
- Font sizes
- Storage sanitization

### CSS Files

1. **globals.css** - Global styles and theme overrides
2. **theme-animations.css** - Theme transition animations
3. **glass-theme.css** - Glass theme specific styles
4. **academic-animations.css** - Academic component animations
5. **light-theme-fixes.css** - Light theme fixes and overrides

---

## Key Features

### 1. **School Detection**
- IP-based school detection
- Personalized content based on school
- Spotlight schools display

### 2. **File Upload**
- Email-based upload
- Drag-and-drop support
- File validation (PDF, Word, images)
- Max size: 15MB

### 3. **Authentication Check**
- Redirects authenticated users to dashboard
- Shows loading state during check
- Prevents flash of content

### 4. **Performance Optimizations**
- Lazy loading for below-fold sections
- Suspense boundaries
- Code splitting
- Reduced motion support

### 5. **Accessibility**
- Reduced motion support
- Keyboard navigation
- ARIA labels
- Semantic HTML

### 6. **Analytics**
- Amplitude event tracking
- Homepage view tracking
- Feature flag support

---

## File Organization

### Core Files

```
src/app/
  ├── page.tsx              # Main landing page
  ├── layout.tsx            # Root layout with theme bootstrap
  ├── fonts.ts              # Font configuration
  ├── globals.css           # Global styles
  └── light-theme-fixes.css # Light theme overrides
```

### Components

```
src/components/
  ├── landing/              # Landing page specific components
  │   ├── HeroSection.tsx
  │   ├── PrimaryCTA.tsx
  │   ├── GetStartedSection.tsx
  │   ├── ProblemSection.tsx
  │   ├── ComparisonSection.tsx
  │   ├── AceSection.tsx
  │   ├── HowItWorksSection.tsx
  │   ├── HowItWorksWithUpload.tsx
  │   ├── SocialProofSection.tsx
  │   ├── SystemsCloud.tsx
  │   └── SectionPattern.tsx
  ├── Aurora.tsx            # Background animation
  ├── NavBar.tsx            # Navigation bar
  ├── StickyMiniNav.tsx     # Sticky navigation
  ├── Footer.tsx            # Footer
  ├── WaitlistModal.tsx     # Waitlist modal
  ├── CampusAmbassadorForm.tsx
  ├── HomepageBlogSections.tsx
  └── system/
      └── AnimatedIcon.tsx  # Animated icon wrapper
```

### Hooks

```
src/hooks/
  └── useInlineFileUpload.ts  # File upload hook
```

### Theme System

```
src/lib/
  ├── theme-config.ts       # Theme configuration
  ├── theme-utils.ts        # Theme utilities
  ├── theme-dom.ts          # DOM manipulation
  ├── theme-animations.css  # Theme animations
  └── glass-theme.css       # Glass theme styles

src/contexts/
  └── ThemeContext.tsx      # Theme context provider
```

### Resources

```
src/resources/
  └── lottieAnimations.ts   # Lottie animation definitions
```

### Library Files

```
src/lib/
  ├── utils.ts              # Utility functions (cn, etc.)
  ├── amplitude.ts          # Analytics
  ├── schoolDetection.ts    # School detection logic
  ├── topCampuses.ts        # Campus list
  └── ensureAccessToken.ts  # Auth utilities
```

---

## Configuration Files

- **tailwind.config.ts** - Tailwind CSS configuration
- **tsconfig.json** - TypeScript configuration
- **package.json** - Dependencies
- **next.config.ts/js** - Next.js configuration
- **postcss.config.ts/js** - PostCSS configuration

---

## Browser Compatibility

### Safari-Specific Fixes

The Aurora component includes Safari-specific fixes:
- Hardware acceleration hints
- Explicit canvas clearing
- Proper alpha compositing
- Transform properties for compositing

### WebGL Fallback

- Desktop: WebGL Aurora component
- Mobile: CSS radial gradient fallback
- Graceful degradation for unsupported browsers

---

## Performance Considerations

1. **Lazy Loading** - Below-fold sections are lazy loaded
2. **Code Splitting** - Components split into separate chunks
3. **Suspense Boundaries** - Loading states for async components
4. **Reduced Motion** - Respects user preferences
5. **Intersection Observer** - Pauses animations when off-screen

---

## Export Script

The export script (`scripts/export-landing-page-files.sh`) captures:
- All component files
- All CSS files
- Theme configuration
- Hooks and utilities
- Resources and animations
- Configuration files
- Recursive dependencies

**Run the export:**
```bash
./scripts/export-landing-page-files.sh
```

**Output:** `LANDING_PAGE_EXPORT.txt`

---

## Summary

The DormWay landing page is a comprehensive, performant, and accessible single-page application featuring:

- **11 landing page sections**
- **20+ supporting components**
- **4 theme modes** (system, light, dark, glass)
- **WebGL Aurora background** with CSS fallback
- **School detection and personalization**
- **Inline file upload** functionality
- **Comprehensive design system** with design tokens
- **Performance optimizations** (lazy loading, code splitting)
- **Accessibility features** (reduced motion, keyboard navigation)

All files are documented and exported via the comprehensive export script for easy review and maintenance.
